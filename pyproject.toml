[project]
name = "jsonify_objects"
version = "1.0.0"
description = "Stdlib function to jsonify almost any Python object recursively"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
